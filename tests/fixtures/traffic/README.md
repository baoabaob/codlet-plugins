Public test-only CA and leaf certificate, copied from the Core process-traffic
fixtures. No private key is included. These are never an installed trust root and
must not be used with real user credentials. Real launches generate fresh keys.
